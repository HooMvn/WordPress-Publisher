{
  "description": "Secret درست ولی eventType نامعتبر -> باید 422 (invalid_payload) برگرداند، نه 401 (Fix #15)",
  "method": "POST",
  "path": "/webhook/wp-post-events-v1",
  "headers": { "X-WP-Webhook-Secret": "${WORDPRESS_WEBHOOK_SECRET}", "Content-Type": "application/json" },
  "body": { "eventType": "post.deleted", "postId": 555 },
  "expected": { "status": 422, "outcome": "invalid_payload" }
}
